using UnityEngine;

// Basically a glorified GameObject.
// For now, contains nothing and is only used for casting to identify true nodes.
// However, in the future I can imagine encoding MovingObject commands (like jump at this node)
// inside PathNode.
public class PathNode : MonoBehaviour {
	
}